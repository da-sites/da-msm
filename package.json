{
  "name": "da-msm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:stage": "wrangler deploy --env stage"
  },
  "devDependencies": {
    "wrangler": "^4.131.2"
  }
}
